<% content_for(:title, @game.title) %>

<h1 class="text-xl font-bold mb-4"><%= @game.title %></h1>

<%= tag.iframe src: embed_game_path(@game, token: @game_attempt_token),
                class: "w-full h-[600px] border-0",
                title: @game.title %>
